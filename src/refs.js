import { createRef } from "react";

export const headerRefs = {
    logo: createRef(),
    navItems: createRef(),
};

export const heroRefs = {
    title: createRef(),
    subtitle: createRef(),
    button: createRef(),
    image: createRef(),
};
